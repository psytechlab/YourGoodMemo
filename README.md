# Anchor Detection — поиск «точек опоры» в Telegram-переписке

ИИ-агент, который читает личную переписку человека и находит в ней **«точки опоры»** — паттерны, связи и события, которые дают ощущение смысла и удерживают в жизни.

---

## Что это такое

«Точка опоры» — конкретный, наблюдаемый паттерн в переписке, указывающий на источник устойчивости. Например:

- **S1** — человек несколько раз за месяц открывается эмоционально, и собеседник отвечает с поддержкой
- **S5** — несколько людей регулярно обращаются к нему за советом или помощью
- **D3** — он упоминает одно и то же хобби каждую неделю с прогрессом и вовлечённостью

Агент находит такие паттерны и формирует тёплый, конкретный текст с прямыми отсылками к переписке.

---

## Архитектура

Пятиступенчатый пайплайн на локальных LLM через [Ollama](https://ollama.com):

```
Telegram export (result.json)
        │
        ▼
┌─────────────────────────────────────────────────────┐
│  Stage 1 — Labeler (qwen2.5:7b)                     │
│  Regex-предфильтр → LLM-валидация кандидатов        │
│  Hybrid: лексикон + полный LLM-скан батчами         │
└──────────────────────┬──────────────────────────────┘
                       │ кандидаты с контекстом
        ▼
┌─────────────────────────────────────────────────────┐
│  Stage 2 — Detector (qwen2.5:14b)                   │
│  P1: S1, S2, S5, D2  │  P2: S3, C1, D3, E2         │
│  Анализирует только отфильтрованные сообщения        │
└──────────────────────┬──────────────────────────────┘
                       │ candidate_evidence
        ▼
┌─────────────────────────────────────────────────────┐
│  Stage 3 — Validator (qwen2.5:14b)                  │
│  Скептично проверяет каждую улику (accepted/rejected)│
└──────────────────────┬──────────────────────────────┘
                       │ подтверждённые опоры
        ▼
┌─────────────────────────────────────────────────────┐
│  Stage 4 — Narrator (mistral-nemo:12b)              │
│  Нарративный текст для каждой опоры с эпизодами     │
└──────────────────────┬──────────────────────────────┘
                       │ нарративы
        ▼
┌─────────────────────────────────────────────────────┐
│  Stage 5 — Composer (mistral-nemo:12b)              │
│  Единый поддерживающий ответ                        │
└─────────────────────────────────────────────────────┘
```

---

## Установка

```bash
# 1. Установить Ollama
# https://ollama.com/download

# 2. Скачать модели
ollama pull qwen2.5:7b-instruct-q4_K_M
ollama pull qwen2.5:14b
ollama pull mistral-nemo:12b

# 3. Установить зависимости Python
pip install pandas requests natasha openpyxl
```

---

## Запуск

```bash
# Экспортируй переписку из Telegram Desktop:
# Настройки → Экспорт данных → формат JSON

# Запуск на одном или нескольких чатах
python run_multi.py data/result.json
python run_multi.py data/chat1.json data/chat2.json data/chat3.json \
    --output results/profile.json \
    --report results/report.txt

# Детальная аналитика по результатам
python analyze.py --profile results/profile.json
```

На Windows перед запуском:
```powershell
$env:PYTHONIOENCODING = "utf-8"
```

---

## Структура проекта

```
course work/
├── anchor_detection/          ← утилиты
│   ├── llm_client.py          ← обёртка Ollama REST API
│   ├── lexicons.py            ← словари для P1-детекторов
│   ├── lexicons_p2.py         ← словари для P2-детекторов
│   ├── lemmatizer.py          ← лемматизация через natasha
│   └── preprocessors.py       ← статистические агрегаты
│
├── multi_pipeline/            ← пятиступенчатый пайплайн
│   ├── config.py              ← все параметры (модели, батчи, таймауты)
│   ├── orchestrator.py        ← оркестрация
│   ├── stage1_labeler.py      ← предфильтрация кандидатов
│   ├── stage2_detector.py     ← детекторы P1 (S1/S2/S5/D2)
│   ├── stage2_p2_detector.py  ← детекторы P2 (S3/C1/D3/E2)
│   ├── stage3_validator.py    ← валидация улик
│   ├── stage4_narrator.py     ← нарративный текст
│   └── stage5_composer.py     ← финальный ответ
│
├── data/
│   ├── result.example.json    ← пример формата входных данных
│   └── synthetic/             ← синтетические чаты для тестирования
│
├── telegram_parser.py         ← парсер Telegram JSON → DataFrame
├── run_multi.py               ← точка входа
├── analyze.py                 ← аналитика результатов
└── CONTEXT.md                 ← подробный контекст разработки
```

---

## 8 реализованных точек опоры

| Код | Название | Приоритет |
|-----|----------|-----------|
| S1 | Эмоциональная поддержка и близкие связи | P1 |
| S2 | Принадлежность к сообществу | P1 |
| S5 | «Я нужен» / ответственность за других | P1 |
| D2 | Финансовая безопасность | P1 |
| S3 | Признание и самооценочная поддержка | P2 |
| C1 | Надежда и оптимизм — конкретные планы | P2 |
| D3 | Хобби, увлечения и поток | P2 |
| E2 | Копинг-репертуар и стратегии регуляции | P2 |

Всего запланировано 18 точек опоры в 7 сферах.

---

## Важно

Файлы с реальными переписками (`data/*.json`) исключены из репозитория через `.gitignore`. Для запуска нужен собственный экспорт из Telegram. Формат входного файла — `data/result.example.json`.
